import { describe, expect, it } from 'vitest'

// Replace this with real unit tests. Lives in `test/unit/` so it picks up the
// `unit` project from vitest.config.ts (node env, no Nuxt context).
describe('sample', () => {
    it('runs', () => {
        expect(1 + 1).toBe(2)
    })
})
