# Vendored brand assets

Copied verbatim from the "Battlestack Design System Nuxt" project on
claude.ai/design (`assets/`). That project is the source of truth; re-copy
from there rather than editing here.

- `battlestack-mark.svg` — the three-bar product mark, `currentColor`.
- `logo.svg` / `logo-dark.svg` / `logo-mark.svg` — SevenLab company logo
  variants, for company-level material only. Product UI uses the Battlestack
  lockup (`<BsLogo>`), never these.
